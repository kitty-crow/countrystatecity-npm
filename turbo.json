{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "outputs": [
        "dist/**"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/src/**"
      ]
    },
    "dev": {
      "persistent": true,
      "cache": false,
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/src/**"
      ]
    },
    "test": {
      "dependsOn": [
        "build"
      ],
      "cache": false,
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/src/**"
      ]
    },
    "typecheck": {
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/src/**"
      ]
    },
    "lint": {},
    "generate-data": {
      "outputs": [
        "$TURBO_ROOT$/src/*/data/**"
      ],
      "cache": false
    },
    "clean": {
      "cache": false
    }
  }
}
