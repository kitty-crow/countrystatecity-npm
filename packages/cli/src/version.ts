/** Published CLI version. Keep this aligned with the package manifest. */
export const VERSION = '2.0.0';

/** User agent sent with API requests. */
export const USER_AGENT = `@countrystatecity/cli/${VERSION}`;
